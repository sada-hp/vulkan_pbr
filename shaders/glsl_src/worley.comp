#version 450

layout(local_size_x = 1, local_size_y = 1, local_size_z = 1) in;

layout (binding = 0) uniform buffer points { vec2 p; }
layout (binding = 1, format = rgba8) uniform image2D outImage;

void main()
{
    
}